<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button
        type="button"
        class="navbar-toggle collapsed"
        data-toggle="collapse"
        data-target="#navbar"
        aria-expanded="false"
        aria-controls="navbar"
      >
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>

      {{link-to "ember-arcgis-server-services" "index" class="navbar-brand"}}

    </div>
    <div id="navbar" class="navbar-collapse collapse">
      <ul class="nav navbar-nav">
        {{#active-link}}{{link-to "Home" "index"}}{{/active-link}}
        {{#active-link}}{{link-to "Harness" "harness"}}{{/active-link}}
        {{#active-link}}{{link-to "Sites" "sites"}}{{/active-link}}
      </ul>
      <ul class="nav navbar-nav navbar-right">
        {{#if session.isAuthenticated}}
          <li><a href="#" {{action "signout"}}>Sign Out</a></li>
        {{else}}
          {{#active-link}}{{link-to "Sign In" "signin"}}{{/active-link}}
        {{/if}}

        <li><a href="https://github.com/Esri/ember-arcgis-server-services">Github</a></li>
      </ul>
    </div>{{!--/.nav-collapse --}}
  </div>{{!--/.container-fluid --}}
</nav>

{{outlet}}
